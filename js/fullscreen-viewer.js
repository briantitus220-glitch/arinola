/* =========================================================
   ARINOLA THEATRE — FULLSCREEN VIEWER
   File: js/fullscreen-viewer.js

   Purpose:
   Placeholder for the future fullscreen gallery/lightbox.
   Later, add data-viewer-image to gallery images and build
   the overlay behaviour here.
========================================================= */

const fullscreenViewerItems = document.querySelectorAll("[data-viewer-image]");

if (fullscreenViewerItems.length > 0) {
  // Future viewer behaviour will live here.
}
